import CustomLayout from "../components/CustomLayout";

const ProfilePage = () => {
  return (
    <CustomLayout active="profile">
      <div>
        <h1>My Profile</h1>
      </div>
    </CustomLayout>
  );
};

export default ProfilePage;
